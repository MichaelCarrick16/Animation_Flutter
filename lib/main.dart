import 'package:animation_flutter/rotate_animation.dart';
import 'package:flutter/material.dart';
import 'multi_animation.dart';
import 'scale_animation.dart';

void main() {
  // runApp(MyApp());
  // runApp(HomeScreen());
  runApp(AnimatedScreen());
}

